// Deprecated placeholder: this file is retained for reference only.
// The project now uses `detail_produk.dart` as the single product detail page.
// If you want this file deleted, I can remove it — currently it's intentionally left
// in the repo to avoid accidental data loss.

// ignore_for_file: file_names, unused_import

import 'package:flutter/material.dart';

class DetailKaosDeprecated extends StatelessWidget {
  const DetailKaosDeprecated({super.key});
  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
