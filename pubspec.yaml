name: flutter_application_1
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies: # PASTIKAN SEMUA PACKAGE ADA DI SINI
  flutter:
    sdk: flutter
    
  # ---------- BLOC/CUBIT (Penyebab Error Sebelumnya) ----------
  flutter_bloc: ^8.1.3 
  bloc: ^8.1.2 
  meta: ^1.11.0 
  
  # ---------- UI/UX & Formatting ----------
  getwidget: ^7.0.0 
  intl: ^0.18.1
  shared_preferences: ^2.5.3 

  # ---------- Tambahan untuk ikon/web/network ----------
  cupertino_icons: ^1.0.2
  url_launcher: ^6.1.10
  device_info_plus: ^12.2.0
  webview_flutter: ^4.13.0
  dio: ^5.9.0

dev_dependencies:
# ... (dev dependencies tetap sama)

# Konfigurasi Aset
flutter:
# ... (Aset tetap sama)